# namima offline ambient renderer (candidate) — pinned dependencies.
#
# NOTE (namima rule 6 — "no dependencies without approval"): these are NEW deps
# for the *candidate* renderer only. They are NOT used by the namima runtime
# (sketch.js / audio.js) and must not be merged to main without human approval.
# Versions are the ones the smoke test was verified against (win-arm64 wheels OK).

numpy==2.4.6        # required now: synthesis + WAV packing + FFT (in tests)

# scipy is not imported by generator.py (its fade window is pure-numpy, so that
# renderer's output stays bit-identical regardless of scipy). It IS required by
# the solfeggio_composer.py candidate, which uses scipy.signal butter/lfilter/
# fftconvolve for its filters and reverb (see docs/solfeggio-composer.md). Also
# reserved for the future add_reference_layer slot (SKILL.md §Future slot).
scipy==1.17.1

# dev-only (not needed to run the renderer; used to run tests/ under pytest):
#   pytest==8.x
